(function (app) {
    'use strict';

    // service provide access for about API
    app.factory("aboutService", [
        function () {

            // #region model
            
            return {
                
            };
            
            // #region public functions

            // #region private functions
        }
    ]);
})(app);